input {
  sqs {
    access_key_id => "<%= @aws_access_key %>"
    secret_access_key => "<%= @aws_secret_key %>"
    region => "<%= @aws_region %>"
    queue => "<%= @sqs_queue %>"
    use_ssl => "false"
    codec => "json"
  }
}
